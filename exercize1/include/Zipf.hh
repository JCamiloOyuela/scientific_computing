#pragma once

#include <iostream>
#include <fstream>

#include <utility> // for std::pair
#include <map>
#include <string>
#include <vector>
#include <algorithm>

void add_word( std::string const & w, std::map<std::string,int> & m );
